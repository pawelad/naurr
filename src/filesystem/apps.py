"""filesystem app config."""

from django.apps import AppConfig


class FilesystemConfig(AppConfig):
    """Django `AppConfig` integration for `filesystem` app."""

    name = "filesystem"
    verbose_name = "Filesystem"
